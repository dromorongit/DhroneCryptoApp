# Frontend environment variables
# Vite requires VITE_ prefix for environment variables to be exposed to the client

# API URL for backend - should point to Railway backend in production
VITE_API_URL=https://dhronecryptoapp-production.up.railway.app/api

# For local development, you can use:
# VITE_API_URL=http://localhost:5000/api